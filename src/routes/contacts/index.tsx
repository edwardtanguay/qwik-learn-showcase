import { component$ } from "@builder.io/qwik"

export default component$(() => {
    return (
        <div>
            <h1 class="text-5xl">Contacts</h1>
            <p class="mt-6 flex item-center justify-center">This is the contacts page.</p>
        </div>
    )
})